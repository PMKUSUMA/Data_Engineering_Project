Prompt1: I have a working Python data pipeline that merges vehicle data and logs into a single table. I want to refactor the loading phase to use a relational schema instead. Can you help me split my single DataFrame into two separate SQLite tables—dim_vehicles and fact_diagnostics—linked by vehicle_id to establish a proper one-to-many relationship?
Prompt2: I am generating synthetic diagnostic data using NumPy's random functions, but when I try to save the vehicle metadata to a JSON file, I get a TypeError: Object of type ndarray is not JSON serializable. How can I modify my ingestion script to convert these specific NumPy arrays into a format the JSON library can handle?